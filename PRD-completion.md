# PRD — 게임 완성 (Completion Sprint)

> 착수: 2026-07-27 · 멀티에이전트 오케스트레이션 · 앱인토스 연동은 이번 범위에서 **제외**
> 상위 정본: `PRD-story-overhaul.md`(서사) · `ENGINE-CONTRACT.md`(엔진·집필 계약)

---

## 0. 착수 시점 진단 (정찰 4팀 결과)

| 영역 | 상태 |
|---|---|
| 서사 | 8루트 × (프롤로그+10화) 프로즈 완성. **11~30화 160화 미집필**(스켈레톤 비트시트는 30/30 완비) |
| 엔진 | **분기·엔딩 개념 자체가 없음.** `ChoiceOption.result`가 항상 본류로 합류하는 완전 수렴형 |
| 아키텍처 | `ui.ts` 1,281줄 God File(export는 `mountGame` 1개). 전역 `let` 20여 개. innerHTML 전면 재생성 |
| 성능 | **코드분할 0** — 8루트 대본 전량이 초기 번들. 집필 후 3배로 커진다. 이미지 lazy 0건. paginate가 매 대사 probe DOM 생성(레이아웃 스래싱) |
| 에셋 | 🔴 **livia 표정 15종이 backup 폴더에만 있어 게임에서 깨짐.** orphan CG 28장, 미제작 CG 62장 |
| 부채 | 구 캐스트 고아 파일 7개(~2,650줄), `handoff.md` 20일 낡음 |

## 1. 완성의 정의 (Definition of Done)

1. **서사** — 8루트 전부 프롤로그+30화 완주 가능. 루트당 GOOD/BAD/TRUE 3엔딩 = **24엔딩**
2. **엔진** — 결의(resolve) 누적 → 15·25·30화 엔딩 게이트 분기. 엔딩 도감·루트 재도전
3. **아키텍처** — `ui.ts` 책임 단위 분해, 루트 데이터 동적 import 코드분할, 이미지 lazy, 죽은 코드 제거
4. **에셋** — livia 복구, CG 레지스트리 전면 정비, 핵심 CG(엔딩 24장 우선) 생성
5. **검증** — `tsc --noEmit` + `npm run build` 통과, playwright 8루트 스모크 콘솔 에러 0

## 2. 오케스트레이션 설계 — 파일 소유권 분할

동시 편집 충돌을 **파일 소유권**으로 원천 차단한다. 어떤 에이전트도 남의 파일을 쓰지 않는다.

| 트랙 | 담당 | 소유 파일 |
|---|---|---|
| 집필 ×8 | 루트별 전담 작가 (Opus) | `src/data/{id}_route.ts` 1개 + `story/cg-manifest-{id}.json` |
| 엔진 | 아키텍처·분기 (Opus) | `src/main.ts`, `src/game/**`, `routes.ts`, `season1.ts`, `chapters.ts`, `style.css`, `vite.config.ts` |
| 에셋 | 이미지·레지스트리 (Sonnet) | `public/**`, `cgs.ts`, `characters.ts`, `ILLUST-CG-LIST.md` |
| 이미지 생성 | codex-imagegen 에이전트 | `public/cg/**` (에셋팀 완료 후) |

**교차 지점 해소**: 집필팀은 CG를 `CG("id")` 호출만 하고 등록은 매니페스트 JSON으로 넘긴다
→ 에셋팀이 `cgs.ts`에 병합. `Gate`/`resolve` 타입 선언은 착수 전 메인이 선반영해
양 팀이 동시에 `tsc`를 통과할 수 있게 했다.

## 3. 웨이브

- **W1 (동시 10)** — 집필 8 + 엔진 + 에셋 ← *진행 중*
- **W2** — CG 이미지 생성 (codex `image_gen`, 엔딩 24장 우선). 매니페스트 확정 후
- **W3** — 통합: `cgs.ts` 병합 결과 × 집필 CG 호출 대조, 루트 목록 문구 갱신(30화 반영)
- **W4** — VERIFY: 정합성 감사(6비트 골격·회귀 누설·금지어), 보안·품질 리뷰, 8루트 플레이 스루 QA
- **W5** — REFINE/SHIP: 대형 파일 분할, 중복 통합, 문서 현행화(`handoff.md`·`MEMORY.md`), 커밋

## 4. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 집필 에이전트가 20화를 다 못 쓰고 중단 | 화 단위 Edit append 지시. 완주 검증 후 갭 발생 시 재투입(Gate 루프) |
| 루트 간 정합성 붕괴(6비트 골격 훼손) | 계약서에 6비트 원문 명시 + W4에서 전 루트 교차 감사 |
| 회귀 비밀 누설 / '악녀' 호칭 | W4 금지어·누설 스캔 |
| 번들 비대화(대본 3배) | 엔진팀 동적 import 코드분할이 W1 필수 항목 |
| CG 62장 미제작 | 엔진에 `.cg-missing` 폴백이 이미 있어 **게임은 완주 가능**. 생성은 엔딩 24장부터 우선순위로 |
